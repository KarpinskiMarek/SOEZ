1) Utworzenie tabeli trip_roles (id, name) relacja 1 - * między trip_roles, a user_trip // zrobione
2) Dodawanie innych userów do tripa -> (usuwanie, modyfikacje) // zrobione i nawet jest walidacja sprawdzajaca czy user jest juz w tripie
3) Manipulacje datami (początek, konic tripa, daty dla placeów)
4) Manipulacja placeami
5) Podpięcie prompta pod details // zrobione, ale sprawdz czy o to Ci chodziło, więcej napisałem poniżej
6) Modyfikacja prompta min: rozszerzenie o długoś pobytu (np. zaplanuj 3 dniowy pobyt w Sosnowcu)
   max: to taki że na froncie są filtry (np. rodzaje atrakcji itp) i prompt te filtry uwzględnia

   1) rodzje atrakcji: bary, muzea, resturacje, // w nastepnym commicie bedzie

7) Utworzenie tabeli prompts zawierającą (id, string prompt) -> request na prompt powoduje zapisanie wyniku
   prompta do tabeli i utworzenie relacji między danym placem a promptem // zrobione, to samo co punkt 5

//dodałem relację Place 1----* Prompt. Moze mogloby to funkcjonować tak że dla danego place będziemy
//generować wiele promptów i to będzie baza z której będziemy mogli zaczerpnąć juz wygenerowane propozycje
//przy tworzeniu nowego place generuje się prompt i zapisuje w polu prompt, jak i w tabeli prompt.
//Nie jest to troche redundantne? Jak możnaby to inaczej rozwiązać?
// A może to właśnie dobrze, bo będzie można się odwołać do tego prompta z bazy kiedy indziej, a tutaj np zmienić?



8) dodac koncowke ktora edytuje nazwe dateod datedo tripa. // to samo co punkt 3

   75$ szacowana cena za 100000 requestów do prompta